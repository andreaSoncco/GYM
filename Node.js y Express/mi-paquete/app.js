/* Package-lock.json
Se genera automáticamente cuando npm modifica el árbol de node_modules o 
package.json. Nos va a permitir mantener un registro del árbol de dependencias
que necesita nuestro paquete. Describe el árbol generado para que futuras
instalaciones puedan generar exactamente el mismo árbol, de esa forma, otros
desarrolladores pueden instalar exactamente las mismas dependencias que tu.  
*/
